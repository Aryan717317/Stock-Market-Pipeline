[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
addopts = "-ra"
markers = [
  "integration: requires the dedicated PostgreSQL test database",
  "airflow: requires the Airflow container image",
]
